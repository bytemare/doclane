version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - bodyclose
    - errorlint
    - misspell
    - unconvert
    - unparam
    - usestdlibvars

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

